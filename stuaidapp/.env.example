# 后端 API 根地址。
# - iOS 模拟器可用 http://localhost:8080
# - Android 模拟器需用 http://10.0.2.2:8080
# - 真机调试需填开发机在局域网内的 IP，例如 http://192.168.1.10:8080
# 复制为 .env.local 后按需修改（Expo 会自动加载 EXPO_PUBLIC_ 前缀的变量）。
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080
# 使用 React Native 原生 fetch（基于 XMLHttpRequest），支持 {uri} 形式的 FormData 文件上传。
# Expo SDK 56 默认的 fetch polyfill 不支持 {uri} 文件 part，上传会报
# "Unsupported FormDataPart implementation"。设为 1 回退到 RN 原生 fetch。
EXPO_PUBLIC_USE_RN_FETCH=1
