# 学生资助系统 — 项目需求与开发计划

> 业务背景：高校学生资助中心《优化学生家庭经济认定与资助流程》研讨纪要，以及《家庭经济困难学生认定申请表（2024年）》《贵州省高等学校国家助学金申请表》。
>
> 本文是需求与进度的单一事实来源。勾选表示**已在对应端落地**（后端 API / Web / 手机端若不一致，会在条目后注明）。

---

## 当前进度（2026-08）

**一期主路径已打通，可用于演示与用户测试。**

已交付：

- 认证与 RBAC（本人 / 本班 / 本系 / 全校）
- 组织机构、字典、行政区划、学生与重点人群（含 Excel 导入导出）
- 困难认定填报、校验、附件、手写签字、三级评审、退回、批量审核
- 国家助学金：认定预填、填报、三级评审、Word 导出
- 认定结果汇总表 Excel 导出（文件名按角色：班级名 / 系名 / 学院）
- 认定申请表 Word 导出（`{姓名}-困难认定申请表.docx`）
- Web 管理端（按角色菜单）与 Expo 手机端（学生填报 + 教师审核）
- Docker Compose 一键部署；backend 启动时 GORM AutoMigrate，二次部署可给旧库加列

未交付（二期或收尾）：

- 公示、名额/预算、励志奖学金等其它资助类型
- 站内通知、审计日志、系统参数配置
- Web 找回密码 / 修改密码页面（后端接口已有）
- 一般困难档排序辅助；与民政 / 乡村振兴数据对接、AI 辅助定档

---

## 一、项目目标

将「家庭经济困难学生认定 → 困难等级评定 → 助学金申请」全流程线上化，解决：

1. **材料不规范**：纸质/手填随意，字段与逻辑对不上。
2. **审核压力集中在资助中心**：应下沉到班级、教学系。
3. **重复劳动**：汇总靠复制粘贴；助学金与认定重复填。
4. **缺少校验**：身份证、人均收入、家庭成员关系、单亲/单薪等需程序约束。
5. **重点保障人群难识别**：脱贫、低保、孤儿等需预导入并自动匹配。

### 分期

- **一期**：填报规范性——下拉与校验、三级评审与退回、Excel 导入导出、助学金预填。**已基本交付。**
- **二期**：审核智能化——历史数据辅助定档、与民政/乡村振兴核验收入。预留扩展，不阻塞当前上线。

---

## 二、角色与权限（RBAC）

| 角色 | 说明 | 主要权限 | Web 默认页 | 手机端 |
| --- | --- | --- | --- | --- |
| 学生 student | 填报认定与助学金、上传材料、看进度、下载通过后的表单 | 仅本人 | `/recognitions` | 资助申请 |
| 班主任 classadvisor | 班级评审（第一级），可退回、初定困难等级 | 本班 | `/reviews` | 审核工作台 |
| 教学系 department | 教学系评审（第二级），可退回 | 本系 | `/reviews` | 审核工作台 |
| 资助中心 aidcenter | 院级终审、汇总导出 | 全校 | `/reviews` | 审核工作台 |
| 管理员 admin | 用户 / 机构 / 字典 / 学生数据 | 全校 | `/dashboard` | 与资助中心类似（审核） |

评审规则：班级 → 教学系 → 资助中心，逐级通过或退回。院级通过后认定完成，学生可发起助学金。管理员不替代学生填报入口，办事走「资助审核」。

---

## 三、核心业务流程

```
学生填报认定申请表（程序化校验 + 手写签字）
   └─> 班级评审 ──退回──┐
        └─> 教学系评审 ──退回──┤
             └─> 院级评审（终审）──退回──┘
                  └─> 认定通过 → 导出 Word 申请表 / 汇总表
                       └─> 学生发起国家助学金（认定表预填）
                            └─> 同样三级评审 → 通过后导出 Word 申请表
```

关键规则：

- **先认定、后资助**：未通过认定不能申请助学金。
- **困难等级**：特别困难 / 比较困难 / 一般困难。
- **导出**：认定申请表、助学金申请表为 Word（docx 模板填数）；认定汇总表为 Excel，文件名按导出人角色。
- **手写签字**：个人承诺印刷展示，学生本人（或监护人）在线手写签字后随申请保存。

---

## 四、功能模块（后端 + 业务）

### 模块 1：认证与权限

- [x] 账号登录（学号/工号 + 密码）、JWT 签发与刷新
- [x] 角色 + 数据范围中间件（本人/本班/本系/全校）
- [x] 修改密码、找回密码（**后端已有**；Web「忘记密码」仍为占位链接，无改密页）
- [x] `/me`、工作台概览 `/dashboard`

### 模块 2：组织机构与基础数据

- [x] 院系 / 专业 / 年级 / 班级：登录用户可读；增删改仅管理员；外键与删除保护
- [x] 组织机构 Excel 导入/导出（模板、表头校验、upsert、逐行错误回显）
- [x] 数据字典：民族、政治面貌、与学生关系、职业、健康状况、特殊群体、困难等级、收入来源等（读全员，写仅管理员；`make seed` 种子）
- [x] 行政区划 12 位国标码；按身份证前 6 位解析省/市/区

### 模块 3：学生与重点人群

> 学生档案与重点人群名单的管理接口仅管理员。学生本人档案走 `GET /students/me`。

- [x] 录取/新生名单 Excel 导入（增量 upsert、模板、逐行错误）
- [x] 重点保障人群名单导入（九大类等；同身份+类型幂等）
- [x] 按学号/身份证自动匹配，命中标记 `is_key_group`
- [x] 学生信息管理（校验、分页筛选）；新增学生可同步创建登录账号（用户名=学号）
- [x] 学生信息 Excel 导出（按当前筛选）

### 模块 4：困难认定申请（核心）

权限：学生增删改/提交本人；评审与管理员按数据范围只读；评审动作见模块 5。

- [x] 在线填报认定申请表（基本情况、家庭成员多行、特殊群体、影响家庭经济状况、个人承诺）
- [x] 数据约束：身份证/手机号、家庭人口与成员数、字典下拉、残疾或未勾选特殊群体须填其他情况、人均收入自动计算、单亲/单薪 warnings
- [x] 附件（支撑材料）上传/列出/下载/删除
- [x] 手写签字（PNG 附件，与支撑材料分开）
- [x] 草稿、提交、撤回、进度与退回原因
- [x] 通过后导出 Word（`{申请人姓名}-困难认定申请表.docx`）
- [x] 列表按 `special_type` 等筛选

### 模块 5：三级评审与退回

- [x] 班级 → 教学系 → 院级；通过/退回；班主任初定困难等级，上级可改
- [x] 退回到指定更低级别，须附意见
- [x] 评审记录；待办 / 已办理 / 全部
- [x] 认定待办支持批量通过/退回
- [x] 认定汇总表导出（班主任=`班级名-困难认定汇总表`，系=`系名-困难认定汇总表`，资助中心/管理员=`学院困难认定汇总表`）
- [ ] 一般困难档排序辅助（一期手动排序，二期可接 AI）

### 模块 6：资助申请（国家助学金）

- [x] 从已通过认定预填，学生核对后提交
- [x] 与认定相同的三级审批（待办 / 已办理 / 全部；助学金无批量）
- [x] 通过后导出 Word 申请表
- [ ] 励志奖学金等其它资助类型（模型预留 `grant_type`）
- [ ] 名额/预算管理

### 模块 7：公示管理

- [ ] 认定/资助结果公示发布、公示期、名单（模型 `Publicity` 已建表，无 API/页面；菜单占位）

### 模块 8：Excel 导入导出

- [x] 导入模板：学生、重点人群、院系/专业/年级/班级
- [x] 导入校验与错误行回显
- [x] 认定汇总表（按角色数据范围）
- [x] 学生名单导出
- [ ] 助学金发放名单/汇总表
- [ ] 重点保障人群统计导出

### 模块 9：通知与提醒

- [ ] 退回/进度/公示站内信（模型 `Notification` 已建表；Web 铃铛与 App「消息」为占位）

### 模块 10：系统管理与审计

- [x] 用户管理：增删改、筛选、重置密码、启停、审核角色绑定班级/院系；不可删/禁自己、至少一名管理员
- [x] 机构与字典管理（见模块 2；Web 已有页面）
- [ ] 操作日志、登录日志、数据变更审计（模型 `AuditLog` 已建表）
- [ ] 系统参数（公示天数、资助项目、评审级数等；菜单占位）

---

## 五、Web 管理端（`frontend/`）

单套路由，按 `role` 分流菜单与 `canAccessPath`。布局：侧栏 + 顶栏面包屑。

### 5.1 全局

- [x] 登录页、双令牌、401 自动刷新、按角色落地页
- [ ] 找回密码、修改密码、个人中心
- [x] 按角色动态导航；未授权重定向
- [x] DataTable（TanStack Table，后端分页）、确认框、Toast、加载/空/错误态
- [x] 字典/机构下拉
- [ ] 通知中心；403/404/500 独立页

### 5.2 按角色页面

- [x] 工作台（年度 KPI、快捷入口）
- [x] 学生：认定多步表单、草稿/提交/撤回、我的申请、附件与签字、通过后下载 Word；助学金从认定发起
- [x] 班主任/教学系/资助中心：认定审核（待办/已办理/全部、批量、特殊群体筛选、汇总表导出）；助学金审核
- [x] 管理员：院系/专业/年级/班级、字典、行政区划、学生与重点人群（导入导出）、用户管理、资助审核
- [ ] 公示、系统设置、班级/系进度看板、一般困难排序

---

## 六、手机端（`stuaidapp/`）

Expo Router，与 Web 共用 `/api/v1`。

- [x] 登录、会话持久化、按角色首页（学生资助入口 / 教师待办角标）
- [x] 学生：认定填报（含签字）、助学金申请、申请进度
- [x] 评审：认定 / 助学金待办与详情审核、通过/退回、表单下载
- [x] 认定汇总表导出（评审角色）
- [ ] 「服务」「消息」Tab 为占位；改密、站内信、公示未做
- [ ] 未做独立管理端（基础数据仍走 Web）

---

## 七、技术架构

| 层 | 选型（以仓库为准） |
| --- | --- |
| 后端 | Go 1.26、Gin、GORM、PostgreSQL 15（兼容 MySQL）、golang-jwt/jwt/v5、excelize、viper、bcrypt |
| Web | Next.js 16 App Router、TypeScript、Tailwind CSS v4、Zustand、react-hook-form + zod、TanStack Table、signature_pad |
| 手机 | Expo 54、React Native、expo-router、Zustand、expo-file-system / sharing、手写签字组件 |
| 部署 | `deploy/docker-compose.yml`：postgres + backend + frontend + nginx；附件卷 `uploads`；库卷 `pgdata`；启动时 AutoMigrate |
| 未采用（计划曾写、代码未上） | zap、swagger、TanStack Query、独立对象存储 |

表结构变更：二次 Docker 部署执行 `docker compose up -d --build` 即可（**不要** `down -v`）。AutoMigrate 能加表/加列，不能可靠地删列、改列名或收紧约束。

---

## 八、数据模型（概览）

| 实体 | 关键字段 |
| --- | --- |
| User | username, password_hash, real_name, role, dept_id, status, phone |
| Department / Major / Grade / Class | 院系—专业—年级—班级 |
| Student | user_id, student_no, name, id_card, class_id, is_key_group |
| SpecialGroup | student_no / id_card, type, source |
| RecognitionApplication | student_id, year, 表单字段, status, current_level, difficulty_level |
| FamilyMember | application_id, relation, occupation, annual_income, health |
| ReviewRecord | application_id, level, reviewer_id, action, opinion |
| GrantApplication | recognition_id, grant_type, year, 预填经济字段, status |
| GrantFamilyMember / GrantReviewRecord | 助学金家庭成员与评审 |
| Attachment | owner_type, owner_id, file_name, path |
| Dict / RegionCode | 字典与区划 |
| Publicity / Quota / Notification / AuditLog | 已建表，业务未接 |

枚举：困难等级（特别/比较/一般困难）；评审级别（班级/系/院）；动作（通过/退回）；认定状态（草稿/待班级/待系/待院/通过/退回）；特殊群体（脱贫、低保、孤儿等）。

---

## 九、API 要点

Base：`/api/v1`。统一响应：`{ "code": 0, "message": "ok", "data": {} }`。完整字段见 [backend/docs/API.md](./backend/docs/API.md)。

```
# 认证
POST /auth/login | /auth/refresh | /auth/recover-password
PUT  /auth/password
GET  /me   GET /dashboard   GET /students/me

# 基础数据（读：登录用户；写：admin）
GET  /orgs/departments|majors|grades|classes
GET  /dicts  /dicts/:type
GET  /region-codes  /region-codes/lookup

# 学生与导入（admin）
CRUD /students  /special-groups
GET  /import/template/:type
POST /import/students|special-groups|departments|majors|grades|classes
GET  /export/students  /export/:orgType

# 认定
CRUD /recognitions
POST /recognitions/:id/submit|withdraw
GET  /recognitions/:id/export          Word
GET  /recognitions/summary-export      Excel
POST /recognitions/:id/attachments

# 认定评审
GET  /reviews/todo  /reviews/records
POST /reviews/:id/pass|reject|withdraw   POST /reviews/batch

# 助学金
CRUD /grants   POST /grants/:id/submit   GET /grants/:id/export
GET  /grant-reviews/todo|records
POST /grant-reviews/:id/pass|reject|withdraw
```

---

## 十、开发计划

### 已完成里程碑

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| M0 工程初始化 | 骨架、配置、DB、JWT、迁移 | 完成 |
| M1 认证与基础数据 | 登录/RBAC、机构、字典、学生与名单导入 | 完成（Web 管理页已齐） |
| M2 认定填报 | 表单、校验、附件、签字、草稿/提交 | 完成（Web + App） |
| M3 三级评审 | 流程、退回、待办/批量、汇总导出 | 完成（缺一般困难排序） |
| M4a 助学金 | 预填、审批、Word 导出 | 完成（仅国家助学金） |
| 部署 | Docker Compose、seed、二次部署 AutoMigrate | 完成 |

### 收尾与二期

| 阶段 | 内容 | 说明 |
| --- | --- | --- |
| M4b | 公示、名额/预算、助学金发放名单 | 模型已有，无页面 |
| M5 打磨 | 找回/改密页、通知、403/404、App「消息/服务」 | 不影响主路径演示 |
| M6 二期 | AI 定档、外部数据核验、更多资助类型、审计与参数配置 | 按校方安排 |

验收仍以会议结论为准：填报规范性优先；智能化不挡一期试用。

---

## 十一、风险与注意事项

- **数据安全**：校内部署；按数据范围隔离；附件在本地磁盘 / Docker 卷。
- **迁移**：AutoMigrate 适合加列；改名/删列/收紧约束需手工 SQL，部署前先 `pg_dump`。
- **合规**：公示期、评审级数尚未做成可配置参数。
- **扩展**：资助类型用 `grant_type` 预留，新项目不要复制一套认定流程。
- **二期 AI**：认定数据已结构化落库，便于后续规则或模型。
