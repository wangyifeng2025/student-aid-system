# 学生资助系统 — 项目需求与开发计划

> 业务背景来源：高校学生资助中心《优化学生家庭经济认定与资助流程》研讨会议纪要，以及《家庭经济困难学生认定申请表（2024年）》《贵州省高等学校国家助学金申请表》两份业务表单。

---

## 一、项目目标

将"家庭经济困难学生认定 → 困难等级评定 → 助学金/奖学金申请"的全流程线上化，解决当前痛点：

1. **材料参差不齐**：学生纸质/手填表格随意，字段不规范、逻辑不自洽（如人均收入与家庭成员对不上）。
2. **审核压力集中在资助中心**：每年 4000~5000 份材料的把关压力集中在校级，应下沉到班级、教学系两道关口。
3. **重复劳动**：汇总表靠人工复制粘贴易出错；助学金表与认定表数据重复填写。
4. **缺少数据校验与提示**：身份证、人均收入、家庭成员关系、单亲/单薪等需要程序化约束与提示。
5. **重点保障人群难以自动识别**：脱贫家庭、低保、孤儿等九大类特殊群体需要预导入并自动匹配提醒。

### 分期目标（与会议结论一致）

- **一期（约 1 个月）**：解决"填报规范性"——数据约束、下拉选项、逻辑校验、三级评审流程、退回机制、Excel 导入导出。**优先交付。**
- **二期（约半年）**：解决"资料审核智能化"——基于历史认定数据辅助困难等级排序/建议（AI 辅助判定），与民政/乡村振兴局数据对接核验收入。**本计划聚焦一期，二期预留扩展点。**

---

## 二、角色与权限（RBAC）

| 角色 | 说明 | 主要权限 |
| --- | --- | --- |
| 学生 student | 填报认定申请、申请助学金、上传支撑材料、查看进度、打印通过后的表单 | 仅本人数据的读写 |
| 班主任/辅导员 classadvisor | 班级评审（第一级），可退回学生重填，初定困难等级 | 本班级学生数据 |
| 教学系经办人 department | 教学系评审（第二级），可退回 | 本系数据 |
| 资助中心 aidcenter | 院级评审（第三级，终审）+ 公示、名额/预算管理、数据导入导出 | 全校数据 |
| 系统管理员 admin | 用户/角色/组织机构/字典/系统配置管理 | 全部 |

> 评审为"逐级把关 + 逐级可退回"：班级 → 教学系 → 院级（资助中心，终审）。任一级可退回至下级或学生重填。院级通过后困难等级认定完成，学生可发起助学金申请。

---

## 三、核心业务流程

```
学生填报认定申请表（程序化校验）
   └─> 班级评审(班主任) ──退回──┐
        └─> 教学系评审 ──退回──┤  退回后学生/下级修改重提
             └─> 院级评审(资助中心，终审) ──退回──┘
                  └─> 困难等级认定通过 → 公示
                       └─> 学生发起助学金/励志奖学金申请（数据自动从认定表拆分预填）
                            └─> 审批通过 → 生成名单/汇总表 → 导出 Excel → 学生打印签字纸质归档
```

关键规则：
- **先认定、后资助**：未通过困难认定的学生不能申请助学金/奖学金。
- **困难等级**：特别困难 / 比较困难 / 一般困难（一般困难为最难认定的第三档，需排序与辅助判定）。
- **公示**：认定结果需公示（规则可配置，如公示期天数）。
- **手写承诺**：个人承诺与签字需本人手写，系统通过后允许打印 PDF/表单供线下签字归档。

---

## 四、功能模块（需求列表）

### 模块 1：认证与权限（Auth & RBAC）
- [x] 账号登录（学号/工号 + 密码）、JWT 签发与刷新
- [x] 角色与权限控制中间件（按角色 + 数据范围：本人/本班/本系/全校）
- [x] 修改密码、找回密码

### 模块 2：组织机构与基础数据（Org & Dict）
- [x] 院系 / 专业 / 年级 / 班级维护（**读取**所有登录用户；**增删改**仅管理员；含外键存在性校验与删除关联保护）
- [x] 组织机构 Excel 导入/导出：模板下载、表头校验、必填/格式校验、按编码/名称增量 upsert、逐行错误回显（行号+列名+原因）、导出当前数据
- [x] 数据字典：民族、政治面貌、户口类型、与学生关系、职业、健康状况、特殊群体类型、困难等级、收入来源等（**读取**所有登录用户；**增删改**仅管理员；提供默认种子数据）
- [x] 行政区划代码：12 位国家统计局区划码维护（树形下级、JSON/内置数据导入），按身份证前 6 位解析省/市/区（供后续填报户籍地）

### 模块 3：学生与重点人群数据（Student & Special Group）
> 本模块全部接口仍仅管理员可操作。
- [x] 录取/新生名单 Excel 导入（开学前预导入，支持每日增量 upsert；模板下载 + 逐行错误回显）
- [x] 重点保障人群名单 Excel 导入（九大类 + 孤儿/事实无人抚养/残疾/烈士子女等；同身份+类型+年度幂等）
- [x] 重点人群自动匹配（按学号/身份证匹配名单，命中即标记学生 `is_key_group`；名单变更自动重算）—— 学生端"建议申请档次"提示横幅留待学生端模块
- [x] 学生信息管理（基本信息、所属班级；身份证/手机号/字典/外键校验，分页与筛选）
- [x] 学生信息 Excel 导出（与导入模板列一致，支持当前筛选条件导出全部匹配记录）

### 模块 4：困难认定申请（Recognition Application）—— 核心
> 后端已完成（一期填报与校验）。权限：学生仅可增删改/提交本人申请；班主任/教学系/资助中心/管理员按数据范围（本班/本系/全校）只读；逐级评审通过/退回属模块 5。PDF 导出需在 `export.pdf_font_path` 配置一个中文 TTF 字体。
- [x] 在线填报《家庭经济困难学生认定申请表》，字段与校验对应业务表单：
  - 基本情况：姓名、性别、出生年月、籍贯（到市/县）、身份证号（格式校验）、家庭人口、手机号、详细通讯地址、邮编、家长手机号
  - 家庭成员（多行）：姓名、年龄、与学生关系（下拉，"关系"非"称呼"，如父女而非父亲）、工作/学习单位、职业（务工/务农/无/读书）、年收入、健康状况（良好/较差/残疾）、特殊群体类型
  - 特殊群体勾选：九大类 + 孤儿/事实无人抚养儿童/残疾学生/残疾人子女/烈士子女
  - 影响家庭经济状况信息：家庭人均年收入、自然灾害、突发意外、残疾/年迈劳动力弱、失业、欠债、其他（无则填"无"）
  - 个人承诺（线上记录承诺勾选，签字线下手写）
- [x] **数据约束与提示**（一期重点）：
  - 身份证 18 位校验；手机号校验
  - 家庭成员人数与"家庭人口"逻辑一致校验（如家庭人口 5 则成员填 4）
  - 关系/职业/健康状况等只能从下拉选择，不能自由填写
  - 单亲/单薪提示：父母仅一方在世/有收入时给出提示与对应政策说明（提交返回 warnings，非阻断）
  - 人均年收入与家庭成员收入、家庭人口的数学逻辑校验（提交时由后端自动计算覆盖）
  - 健康状况选"残疾"时，"其他情况"必须说明；未勾选九大类必须在"其他情况"说明
- [x] 附件上传（住院证明、低保证明、灾情材料等支撑材料；本地磁盘存储 + 上传/列出/下载/删除，大小与扩展名约束）
- [x] 草稿保存、提交、查看进度与退回原因（草稿/被退回可编辑重提；列表/详情含状态、当前级别与退回意见）
- [x] 通过后导出/打印认定申请表（PDF；仅认定通过后可导出，需配置中文字体）

### 模块 5：三级评审与退回（Review Workflow）
> 后端已完成。权限：评审角色（班主任/教学系/资助中心）与管理员；各自只处理本级别且在数据范围内的申请，每次动作写入流转记录。
- [x] 评审流程引擎：班级 → 教学系 → 院级（终审），逐级通过/退回
- [x] 班主任初定困难等级（特别困难/比较困难/一般困难）；上级可调整
- [x] 退回到指定级别（学生/班主任/系/院级），附退回意见（须低于当前级别）
- [x] 评审记录与流转日志（审计；详情含评审人、动作、意见、时间）
- [x] 待办列表 + 批量审核（边看边打勾，快速定档；批量返回逐条结果）
- [ ] 一般困难档排序辅助（一期提供手动排序/打分，二期接 AI）

### 模块 6：资助申请（Grant / Scholarship）
- [ ] 助学金（国家助学金）申请：数据从认定表自动拆分预填，学生补全缺项即可
- [ ] 励志奖学金等其他资助类型（可扩展的资助项目配置）
- [ ] 资助申请审批流
- [ ] 名额/预算管理（按上级指标分配，统计已分配/剩余）

### 模块 7：公示管理（Publicity）
- [ ] 认定/资助结果公示发布、公示期配置
- [ ] 公示名单生成与查看

### 模块 8：Excel 导入导出（Import / Export，基于 excelize）
- [ ] 导入模板下载（录取名单、重点人群名单）
- [ ] 导入校验与错误行回显（哪一行哪一列不合规）
- [ ] 认定汇总表导出（按班级/系/全校）
- [ ] 助学金发放名单/汇总表导出
- [ ] 重点保障人群统计导出

### 模块 9：通知与提醒（Notification）
- [ ] 退回提醒、审核进度提醒、公示提醒（站内信，预留短信/微信扩展）

### 模块 10：系统管理与审计（System & Audit）
- [x] 用户管理：增删改查、按角色/状态/关键字过滤、重置密码、启用/禁用（仅管理员）；为审核角色绑定数据范围（班主任→班级、教学系→院系）；保护性约束：不可删除/禁用自己、系统至少保留一名管理员
- [ ] 角色/机构/字典管理（机构与字典管理见模块 2）
- [ ] 操作日志、登录日志、数据变更审计
- [ ] 系统参数配置（公示天数、资助项目、评审级数开关等）

---

## 五、技术架构

### 后端（本目录）
- 语言/框架：**Golang + Gin**
- ORM：**GORM**（PostgreSQL 14+，亦兼容 MySQL）
- 认证：**golang-jwt/jwt（jwt-go）**
- Excel：**xuri/excelize**
- 校验：go-playground/validator
- 配置：viper（yaml + 环境变量）
- 日志：zap
- 密码哈希：bcrypt
- 文档：swagger（swaggo，可选）

### 前端管理页（`frontend/` 目录）
- 框架：**Next.js（App Router）+ TypeScript**
- UI：**shadcn/ui + TailwindCSS**（浅色主题为主，桌面优先、兼容平板）
- 状态：**Zustand**（鉴权 / UI / 草稿等本地状态）；服务端数据用 **TanStack Query**（请求缓存、失效、重试）
- 表单与校验：**react-hook-form + zod**，校验规则与后端 `validator` 对齐（身份证、手机号、人均收入逻辑等）
- 请求层：统一 `fetch` 封装，自动注入 JWT、`401` 自动刷新令牌、按统一响应 `code` 处理错误并 Toast 提示
- 权限：基于 `/me` 返回的 `role + permissions + data_scope`，实现路由守卫 + 菜单/按钮级权限控制
- 目录约定：`src/app`（路由，按 `(auth)`/`(dashboard)` 分组）、`src/components`（含 `ui/` shadcn）、`src/lib`（api、utils）、`src/store`（zustand）、`src/hooks`、`src/types`

### 部署
- 校内部署（数据不外借），Docker 化，Nginx 反向代理，PostgreSQL + 对象存储/本地磁盘存放附件。

---

## 六、数据模型（概览）

| 实体 | 关键字段 |
| --- | --- |
| User 用户 | id, username(学号/工号), password_hash, real_name, role, dept_id, class_id, status |
| Department/Major/Grade/Class 机构 | 院系/专业/年级/班级层级 |
| Student 学生 | user_id, student_no, gender, birth, nation, political_status, id_card, phone, enroll_time, class_id |
| SpecialGroup 重点人群 | student_no/id_card, type(九大类等), source, batch, year |
| RecognitionApplication 认定申请 | student_id, year, 基本/家庭经济/影响信息字段, status, current_level, difficulty_level, family_population, household_type, per_capita_annual_income |
| FamilyMember 家庭成员 | application_id, name, age, relation, work_unit, occupation, annual_income, health, special_type |
| ReviewRecord 评审记录 | application_id, level, reviewer_id, action(pass/reject), opinion, difficulty_level, created_at |
| GrantApplication 资助申请 | student_id, recognition_id, grant_type, year, amount, status, fields(预填自认定表) |
| Attachment 附件 | owner_type, owner_id, file_name, path, size, mime |
| Publicity 公示 | type, title, content, start_at, end_at, status |
| Quota 名额预算 | year, grant_type, scope, total, used |
| Dict 字典 | type, code, label, sort |
| AuditLog 审计 | user_id, action, target, detail, ip, created_at |
| Notification 通知 | user_id, title, content, is_read, created_at |

> 枚举：困难等级（特别困难/比较困难/一般困难）、评审级别（班级/系/院）、评审动作（通过/退回）、申请状态（草稿/待班级/待系/待院/通过/退回）、特殊群体类型（脱贫/脱贫不稳定/边缘易致贫/突发严重困难/低保/低保边缘/特困救助供养/刚性支出困难/其他低收入/孤儿/事实无人抚养/残疾学生/残疾人子女/烈士子女）。

---

## 七、API 设计（一期主要端点示例）

```
POST   /api/v1/auth/login                 登录
POST   /api/v1/auth/refresh               刷新令牌
GET    /api/v1/me                          当前用户

# 字典/机构
GET    /api/v1/dicts/:type
GET    /api/v1/orgs/classes

# 认定申请
POST   /api/v1/recognitions               提交/暂存认定申请
GET    /api/v1/recognitions               列表（按角色数据范围）
GET    /api/v1/recognitions/:id           详情
PUT    /api/v1/recognitions/:id           修改
POST   /api/v1/recognitions/:id/submit    提交评审
GET    /api/v1/recognitions/:id/export    导出PDF

# 评审
GET    /api/v1/reviews/todo                待办
POST   /api/v1/reviews/:id/pass            通过(可带困难等级)
POST   /api/v1/reviews/:id/reject          退回(带级别+意见)
POST   /api/v1/reviews/batch               批量审核

# 资助申请
POST   /api/v1/grants                      发起资助申请(预填)
GET    /api/v1/grants
POST   /api/v1/grants/:id/audit            审批

# 导入导出
GET    /api/v1/import/template/:type       下载模板
POST   /api/v1/import/students             导入录取名单
POST   /api/v1/import/special-groups       导入重点人群
GET    /api/v1/export/recognitions         导出认定汇总
GET    /api/v1/export/grants               导出资助名单

# 附件/公示/系统
POST   /api/v1/attachments
GET    /api/v1/publicity
... 用户/角色/字典/审计
```

统一响应：`{ "code": 0, "message": "ok", "data": {...} }`。

---

## 八、开发计划（里程碑）

| 阶段 | 周期 | 交付内容 |
| --- | --- | --- |
| M0 工程初始化 | 第 1~2 天 | Go 项目骨架、配置/DB/JWT/统一响应/中间件、健康检查、模型与迁移 |
| M1 认证与基础数据 | 第 1 周 | 登录/RBAC、机构、字典、学生与重点人群 Excel 导入 |
| M2 认定填报 + 校验 | 第 2 周 | 认定申请 CRUD、全部数据约束与逻辑校验、附件上传、草稿/提交 |
| M3 三级评审 + 退回 | 第 3 周 | 评审流程引擎、退回机制、困难等级、待办与批量审核、审计 |
| M4 资助申请 + 导出 + 公示 | 第 4 周 | 助学金预填申请与审批、汇总表/名单导出、公示、通知 |
| M5 联调与试运行 | 第 5 周 | 前后端联调、用 10~20 份真实样例数据测试、上线试运行（先解决规范性） |
| M6（二期） | 后续半年 | AI 辅助困难排序/判定、民政/乡村振兴数据核验、移动端填报优化 |

> 与会议结论对齐：**今年优先确保填报规范性（一期一个月内可交付演示版，两周内出演示 Demo）**，资料审核智能化作为二期长期目标。

---

## 九、前端管理页需求列表

> 前端为单页管理后台（SPA 风格 + App Router），按角色呈现不同导航与页面。下列需求与第四节后端模块一一对应，勾选状态随开发推进更新。

### 9.0 全局与通用（Layout & Common）
- [ ] 登录页（学号/工号 + 密码），登录态持久化与自动续期
- [ ] 找回密码（用户名 + 手机号）、修改密码
- [ ] 全局布局：左侧导航（按角色/权限动态生成）、顶部栏（面包屑、通知铃铛、用户菜单/退出）
- [ ] 路由守卫：未登录跳登录页；无权限跳 403；按 `data_scope` 控制可见数据
- [ ] 通用组件：服务端分页/筛选/排序 `DataTable`、确认弹窗、`Toast`、加载/空态/错误态、表单步骤条 `Stepper`、附件上传组件
- [ ] 字典/机构联动下拉：统一从 `/dicts/:type`、`/orgs/*` 拉取并缓存（全局复用）
- [ ] 全局错误处理：网络异常、`401` 续期、业务 `code` 错误提示
- [ ] 个人中心（基本信息、修改密码）
- [ ] 通用页面：403 / 404 / 500 / 加载骨架屏

### 9.0 导航与路由（按角色）

- [x] 学生：工作台 + 困难认定申请（填报/我的申请）；登录后默认进入 `/recognitions`
- [x] 班主任/教学系/资助中心：工作台 + 待办审核；登录后默认进入 `/reviews`
- [x] 管理员：全量菜单（含基础数据、学生管理、困难认定、待办审核）
- [x] 路由守卫：未授权路径重定向至角色默认首页

### 9.1 学生端（Student）
- [ ] 工作台首页：填报状态卡片、退回提醒、重点人群"建议申请档次"提示横幅
- [x] 困难认定申请填报（多步表单）：基本信息 → 家庭情况（家庭成员可增删多行 + 特殊群体勾选 + 其他说明）→ 经济影响 → 提交确认（个人承诺 + 附件）
  - [x] 前端实时校验：身份证 18 位、手机号；家庭成员数与"家庭人口"一致性；人均年收入自动计算与异常提示；单亲/单薪提示
  - [x] 关系/职业/健康状况/特殊群体等字段一律下拉选择（因 `/dicts` 仅管理员，前端内置与种子对齐的编码-标签常量）
  - [x] "残疾"或未勾选九大类时强制填写"其他情况"说明（提交校验）
- [x] 草稿保存 / 提交评审（提交前均为草稿可编辑；正式撤回随模块 5 流转上线）
- [x] 我的认定申请：列表、详情、进度时间线（当前所处级别）、退回原因展示与重新提交（编辑被退回件后再提交）
- [x] 附件上传与管理（类型/大小限制、上传 / 下载 / 删除；上传进度与在线预览待后续增强）
- [ ] 助学金/奖学金申请：从认定表预填、补全缺项后提交、查看审批进度
- [x] 通过后表单 PDF 下载打印

### 9.2 班主任 / 辅导员（Classadvisor）
- [ ] 班级填报进度看板：未填 / 草稿 / 已提交 / 被退回 统计
- [x] 待办审核列表（本班，支持筛选/搜索）
- [x] 认定申请详情审核：通过（初定困难等级）/ 退回（指定退回对象 + 意见）
- [x] 批量审核 / 快速定档（边看边打勾的高效审核界面）
- [ ] 一般困难档排序辅助（手动拖拽/打分排序）

### 9.3 教学系经办人（Department）
- [x] 系级待办审核列表（本系）、详情审核、通过/退回
- [ ] 系级填报与评审进度统计

### 9.4 资助中心（Aidcenter）
- [x] 院级评审待办、详情审核、通过/退回（院级通过即认定通过，学生可发起助学金申请）
- [ ] 公示管理：发布公示、公示期配置、公示名单查看
- [ ] 名额/预算管理：按指标分配、已分配/剩余统计
- [ ] 数据导入：模板下载、文件上传、导入校验结果与错误行回显（定位行列）
- [ ] 数据导出：认定汇总表（班级/系/全校）、助学金发放名单、重点保障人群统计（导出下载）
- [ ] 资助项目配置（助学金/励志奖学金等可扩展）

### 9.5 系统管理员（Admin）
- [x] 用户管理（增删改、重置密码、状态启停、角色分配、数据范围绑定）：列表分页/筛选、新增/编辑弹窗、删除与重置密码、左侧「系统管理 → 用户管理」入口与路由守卫
- [x] 组织机构管理：院系 / 专业 / 年级 / 班级（含 Excel 导入/导出）
- [ ] 数据字典管理（各类下拉选项维护）
- [ ] 系统参数配置（公示天数、资助项目、评审级数开关等）
- [ ] 审计日志 / 登录日志查询

### 9.6 非功能需求（前端）
- [ ] 响应式布局（桌面优先，兼容平板）；中文界面
- [ ] 基本可访问性（键盘可达、对比度、表单标签）
- [ ] 表单防重复提交、长列表虚拟化/分页、请求并发与缓存控制
- [ ] 统一的加载/空/错误态与友好文案

---

## 十、前端开发计划（里程碑）

> 与第八节后端里程碑并行推进，前端在对应模块 API 就绪后跟进（约滞后 1~2 天）；接口未就绪时先按 API 契约用 mock 联调。

| 阶段 | 周期 | 交付内容 |
| --- | --- | --- |
| FE-M0 工程初始化 | 第 1~2 天 | Next.js+TS 脚手架、Tailwind/shadcn 接入、目录约定、API 封装（JWT/刷新/错误处理）、Zustand 鉴权 store、布局骨架、登录页、路由守卫 |
| FE-M1 基础数据与管理 | 第 1 周 | 登录/找回/改密、全局布局与动态菜单、字典/机构下拉封装、用户/机构/字典管理页（管理员）、数据导入页基础框架 |
| FE-M2 认定填报 + 校验 | 第 2 周 | 多步认定填报表单、全部前端校验与提示、附件上传、草稿/提交、我的申请列表/详情/进度时间线 |
| FE-M3 评审与退回 | 第 3 周 | 各级评审待办、详情审核、通过/退回、批量审核与快速定档、困难等级、班级/系进度看板、一般困难排序 |
| FE-M4 资助 + 导入导出 + 公示 | 第 4 周 | 助学金预填申请与审批、公示管理、名额/预算、导入错误行回显、导出下载、通知中心 |
| FE-M5 联调与打磨 | 第 5 周 | 前后端联调、真实样例数据走查、PDF 打印、错误/空态完善、UI 打磨、试运行 |

> 验收基准与一期目标对齐：**两周内出可演示的认定填报 + 校验 Demo**，一个月内交付覆盖填报、评审、资助、导出的演示版。

---

## 十一、风险与注意事项
- 数据安全：校内部署，附件与个人敏感信息加密/脱敏，严格数据范围隔离。
- 流程合规：评审级数、退回规则、公示期需按省/校文件可配置。
- 可扩展：资助项目（助学金/励志奖学金/学费补偿/退役士兵等）做成配置化的"资助项目"。
- 二期 AI：保留历史认定数据结构化存储，便于后续训练/规则化排序。
