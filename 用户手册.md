# 学生资助系统 · 用户手册

面向日常使用人员。开发与部署见根目录 [README.md](./README.md)、[deploy/README.md](./deploy/README.md)。

手册按角色拆开，但**所有人先读第 1、2 章**。第 2 章讲清楚院系、班级、学生、账号之间的依赖，以及导入导出时最容易踩的坑。

---

## 1. 系统在做什么

本系统处理两件连续的事：

1. **家庭经济困难学生认定**：学生填报 → 班主任审 → 教学系审 → 资助中心终审，确定困难等级。
2. **国家助学金申请**：只有认定通过后，学生才能申请；再走同一套三级审核。

原则：**先认定、后资助**。认定没过，助学金入口会拒绝创建。

### 1.1 角色与能看见什么

| 角色（登录角色名） | 谁在用 | 默认入口 | 能看的数据 |
| --- | --- | --- | --- |
| 学生 `student` | 学生本人 | 困难认定 | 仅本人申请 |
| 班主任 `classadvisor` | 班主任 / 辅导员 | 认定审核 | 所管班级 |
| 教学系 `department` | 系部经办人（手册称「系管理员」） | 认定审核 | 本系 |
| 资助中心 `aidcenter` | 校级资助中心 | 认定审核 | 全校 |
| 管理员 `admin` | 系统管理员 | 工作台 | 全校；可维护基础数据、用户、备份 |

同一人不要兼多个角色账号混用。班主任的「能审哪些班」来自**班主任名册里挂了哪些班**，不是用户管理里随便填一个院系就能审全系。

### 1.2 登录

浏览器打开部署地址（本地开发一般是 `http://localhost:3000`，Docker 演示一般是 `http://localhost:8088`）。

- 学生：用户名是**学号**。首次密码在导入/新增学生时自动生成，规则见下表。
- 班主任、系管理员、学院管理员（资助中心）：用户名按用户管理、班主任名册或导入表填写；初始密码按角色 + 手机号后 6 位生成，**建账号时必须填写手机号**。
- 系统管理员 `admin`：在用户管理里由开户人当场设定密码，不适用下表前缀规则。
- 忘记密码：请系统管理员在「用户管理」里重置；不要互相借用账号审核。

演示环境种子账号见根目录 README，**生产环境必须改掉演示密码**。

#### 初始密码规则

手机号只取数字，取后 6 位。手机号缺失或不足 6 位时无法按规则生成，请补全后再建账号或重置。

| 角色 | 前缀 | 规则 | 示例（手机号 `13800001234`） |
| --- | --- | --- | --- |
| 学生 | `Stu` | `Stu` + **身份证号**后 6 位（身份证不足 6 位时为 `Stu@12345`） | 身份证 `…201234` → `Stu201234` |
| 班主任 | `Adv` | `Adv` + **手机号**后 6 位 | `Adv001234` |
| 系管理员（教学系） | `Dept` | `Dept` + **手机号**后 6 位 | `Dept001234` |
| 学院管理员（资助中心） | `Aid` | `Aid` + **手机号**后 6 位 | `Aid001234` |
| 系统管理员 | — | 开户时手工填写，不自动拼前缀 | — |

---

## 2. 数据逻辑关系（导入导出必读）

系统里的数据不是互相独立的表格。后面一张表引用前面一张表的名称或编码。顺序错了、名称写错一个字，导入就会整行失败。

### 2.1 谁依赖谁

```text
字典（民族、政治面貌、关系、职业、健康、特殊群体类型、困难等级……）
行政区划（身份证前 6 位解析户籍地）
        │
        ▼
      院系 ──────────────┐
        │                │
        ▼                │
      专业（必须填已有「院系编码」）
        │                │
      年级（入学年份）     │
        │                │
        ▼                │
    班主任名册（必须已有院系；班级可后挂）
        │                │
        ▼                │
      班级（必须已有：院系编码 + 专业编码 + 入学年份 + 教工号）
        │
        ▼
      学生（院系 / 专业 / 班级填「名称」；民族、政治面貌填字典「中文名称」）
        │
        ├── 自动创建学生登录账号（用户名 = 学号）
        ├── 重点人群名单（学号或身份证匹配学生，命中后学生标记为重点人群）
        └── 困难认定申请 → 通过后才能申请助学金
```

删除也受这张图约束：院系下还有专业、班级或学生时，系统会拒绝删除。先处理下游，再删上游。

### 2.2 推荐初始化 / 批量导入顺序

新学年或新部署，请严格按下面顺序做。每一步先**下载模板**，按表头原样填写，不要改列名、不要合并单元格、不要用 `.xls`（必须 `.xlsx`）。

| 顺序 | 页面 | 导入时关键字段 | 必须先存在 |
| ---: | --- | --- | --- |
| 1 | 字典管理 / 行政区划 | 一般用系统预置即可 | — |
| 2 | 院系管理 | 院系名称、院系编码 | — |
| 3 | 专业管理 | **院系编码**、专业名称、专业编码 | 院系 |
| 4 | 年级管理 | 年级名称、入学年份 | — |
| 5 | 班主任信息 | 系部、**教工号**、姓名；班级名称可留空 | 院系 |
| 6 | 班级管理 | 院系编码、专业编码、入学年份、班级名称、**教工号** | 院系、专业、年级、该教工号的班主任 |
| 7 | 学生信息 | 学号、姓名、性别、身份证；院系/专业/班级填**名称** | 院系、专业、班级；字典中的民族/政治面貌名称 |
| 8 | 重点人群名单 | 学号或身份证 + 类型编码 | 学生名册（用于自动匹配） |
| 9 | 用户管理 | 教学系 / 资助中心 / 管理员账号 | 院系（审核角色建议填所属院系编码） |

班主任和班级互相引用，正确做法是：

1. 先导入班主任，**班级名称留空**；
2. 再导入班级，教工号填上一步的工号，系统会把班挂到这位老师；
3. 若一位老师带多个班，可在「班主任信息」里再补班级，或多个班级行使用同一个教工号。

不要先导班级再导班主任：班级导入要求教工号已存在。也不要先导学生：学生行里的「信息工程学院 / 软件工程 / 软工2401班」必须和系统里的名称**完全一致**（含空格、括号、级号）。

### 2.3 名称还是编码：两套写法不要混

这是导入失败最常见的原因。

| 导入对象 | 填编码 | 填中文名称 |
| --- | --- | --- |
| 专业、班级 | 院系编码、专业编码、入学年份 | 班级名称 |
| 班主任 | — | 系部可用名称或能解析的关键字；班级名称必须已存在 |
| 用户 | 所属院系编码 | 角色填英文：`student` / `classadvisor` / `department` / `aidcenter` / `admin`；状态填「启用」「禁用」 |
| 学生 | — | 院系、专业、班级、民族、政治面貌一律填**系统里已有的中文名称** |
| 重点人群 | 类型填字典编码，如 `poverty` | 姓名仅作核对 |

学生导出的 Excel 列与导入模板一致，导出后改完可以再导回去（按学号增量更新）。专业、班级导出带的是编码，不要把学生表里的「信息工程学院」填进专业导入的「院系编码」列。

### 2.4 导出三个按钮分别导出什么

各列表工具栏第二行不再使用下拉，而是三个独立按钮：

| 按钮 | 导出范围 |
| --- | --- |
| 导出全部 | 当前模块全部数据（学生等受角色数据范围限制） |
| 导出筛选 | 仅当前查询条件命中的数据；没有筛选时不出现此按钮 |
| 导出勾选 | 仅表格里勾选的行；未勾选时按钮不可用 |

用途建议：

- 备份或移交名册：导出全部。
- 只处理某个院系、某个班：先筛选再导出筛选。
- 抽查或局部修正：勾选后再导出勾选。

导出文件可再导入，但：

- **用户导入不会覆盖已存在的用户名**（避免把密码改掉）。要改角色或院系，请在页面上改，或先在用户管理里处理。
- **学生导入按学号 upsert**：同一学号再次导入会更新该生信息，并尝试同步登录账号。学号填错等于改到另一个人。
- 一次导入会逐行校验，失败行会列出「第几行、哪一列、什么原因」。不要看到成功数就关掉，先看失败清单。

### 2.5 账号是怎么长出来的

| 对象 | 账号怎么来 | 用户名 | 初始密码 |
| --- | --- | --- | --- |
| 学生 | 新增或导入学生时**自动创建**，不要在用户管理里手工开学生号 | 学号 | `Stu` + 身份证后 6 位 |
| 班主任 | 建议在「班主任信息」建名册（会按手机号生成登录密码）；班级范围以名册为准 | 通常用工号 | `Adv` + 手机号后 6 位 |
| 系管理员（教学系） | 在「用户管理」创建或导入，角色 `department`，必须填所属院系和手机号 | 自定 | `Dept` + 手机号后 6 位 |
| 学院管理员（资助中心） | 在「用户管理」创建或导入，角色 `aidcenter`，必须填手机号 | 自定 | `Aid` + 手机号后 6 位 |
| 系统管理员 | 只在「用户管理」创建 | 自定 | 开户时手工设定 |

注意：

- 学号已被某个非学生账号占用时，系统无法再为学生自动开号，导入会失败。
- 改学生学号会尝试同步登录用户名；学生正在用旧学号登录时，改完要通知对方用新学号。
- 班主任用户必须挂到正确院系，并且名册里有班级，否则待办是空的——不是系统坏了，是数据范围为空。

### 2.6 附件不在数据库里

认定申请的证明材料、学生签字图存在服务器上传目录（Docker 卷 `uploads`），库里的 `attachments` 只记路径。因此：

- 只备份数据库、不备份附件目录，恢复后记录还在、文件打不开。
- 请用「系统管理 → 数据备份」做全量备份（默认包含附件），并**下载 zip 到本机或云盘**。服务器损坏时，磁盘上的备份卷也可能一起没。

---

## 3. 管理员的基础数据管理

本章给负责「把学校架子搭起来」的管理员。菜单在 **基础数据管理**、**学生管理**。

### 3.1 院系

- 每个院系要有稳定的**院系编码**（如 `CS`）。后面专业、班级、用户导入都靠它，中途不要随便改。
- 名称给学生导入、班主任导入、页面展示用，改名后学生导入必须改用新名称。
- 有下属专业、班级、学生时不能删除。

### 3.2 专业

- 必须属于某个院系：导入列是「院系编码 + 专业名称 + 专业编码」。
- 同一院系下专业名称、专业编码不要重复。
- 班级、学生都挂在专业上，删专业前先处理班级。

### 3.3 年级

- 「年级名称」如 `2024级`，「入学年份」如 `2024`。班级导入用的是入学年份数字，不是「2024级」这四个字。
- 新生入学后先增年级，再导班级。

### 3.4 班主任信息

- **教工号全局唯一**，班级导入靠它认人。Excel 里教工号、电话请设为文本，避免变成科学计数法。
- **电话必填**：登录初始密码是 `Adv` + 手机号后 6 位，号填错等于密码发错。
- 一位老师可以带多个班。
- 在「班主任信息」新增或导入老师时，系统会按上述规则初始化登录密码。名册解决「管哪些班」，账号解决「能不能登录」。

### 3.5 班级

- 导入列：院系编码、专业编码、入学年份、班级名称、教工号。五项都要能对上已有数据。
- 班级名称必须与学生导入时写的完全一致。建议全校统一命名（如 `软工2401班`），不要同一班出现「软件2401」「软工 2401」两种写法。
- 班主任是班级评审的数据范围来源：班没挂老师，该班学生交了申请，班主任待办也看不到。

### 3.6 字典

下拉选项的唯一来源，包括民族、政治面貌、家庭关系、职业、健康状况、特殊群体类型、困难等级等。

- 学生导入的「汉族」「共青团员」必须能在字典里按**名称**找到。
- 重点人群导入的类型填**编码**（如 `poverty`），不是「脱贫家庭学生」。
- 不要删除正在用的字典项；改名称等于让以后的导入对不上旧 Excel。
- `make seed` / Docker seed 会写入一套默认字典，新环境先看一眼再补本校用语。

### 3.7 行政区划

用于按身份证前 6 位解析户籍地。新环境可导入系统内置全国区划。一般不用每年重导；缺码再补。

### 3.8 学生信息

- 必填：学号、姓名、性别（只能「男」「女」）、身份证号。学号、身份证全局唯一。
- 院系、专业、班级填名称，且班级必须属于该院系。
- 导入按学号更新：可重复导入修正班级、手机号等；**不要用导入当删除**。
- 新增/导入成功即有登录账号。请把初始密码规则告知学生，并要求其登录后修改（若前端暂无改密页，由管理员重置）。
- 评审角色在本页只能看、不能改。管理员才能增删改和导入。
- 「导出全部 / 筛选 / 勾选」与列表筛选一致；班主任、系管理员导出时也只能出自己范围内的学生。

### 3.9 重点人群名单

民政、乡村振兴等外部名单导入后，系统按学号或身份证去匹配学生，命中则学生标记为重点人群，认定填报时会提示。

- 名单本身不是认定结果，只是提醒。
- 类型必须是字典 `special_group_type` 里的编码。
- 先有学生再导名单，否则匹配不上。名单变更后系统会重算匹配。

### 3.10 基础数据开工检查清单

1. 院系、专业、年级、班主任、班级数量对得上教务名册。
2. 每个班都有教工号，对应班主任能在「班主任信息」里看到该班。
3. 抽 3 名学生：院系/专业/班级名称与导入表一致，能用学号登录。
4. 用班主任账号登录，在「学生信息」里能看到本班、看不到外班。
5. 用教学系账号登录，能看到本系各班。

---

## 4. 业务审核过程

认定和助学金都是三级：**班级 → 教学系 → 资助中心**。任一级可通过、可退回。管理员也可审，数据范围为全校。

### 4.1 困难认定状态

| 状态 | 含义 | 谁处理 |
| --- | --- | --- |
| 草稿 | 学生未提交或已撤回 | 学生继续改 |
| 待班级评审 | 已提交 | 该班班主任 |
| 待教学系评审 | 班级已通过 | 该生所在系经办人 |
| 待院级评审 | 系里已通过 | 资助中心 |
| 通过 | 三级都过，困难等级已定 | 学生可据此申请助学金、导出认定表 |
| 退回 | 被退到学生或更低一级 | 按退回目标重填或重审 |

困难等级：特别困难、困难、一般困难。班级、系、资助中心都可以定或调整，最终以通过时的等级为准。

### 4.2 学生侧（审核人员需要知道的门槛）

学生在 Web 或手机端填报。提交前系统会卡这些条件，卡死的申请不会进入班主任待办：

- 基本情况、家庭人口、家庭成员人数 = 家庭人口 − 1（不含学生本人）。
- 户口类型、通讯地址等必填项完整。
- 未勾选特殊群体时，必须在「其他情况」说明困难原因；成员健康为残疾时也必须说明。
- 必须勾选个人承诺，并上传签字图（文件名固定为 `student_signature.png`，一般由签字板生成）。
- 部分特殊群体类型还要求上传低保等证明。

已提交、班级尚未审时，学生可以撤回，申请回到草稿。班级一旦审核，学生不能再改内容，只能等退回。

### 4.3 审核人员怎么做

1. 打开「资助审核 → 困难认定审核」（或班主任/系管理员登录后的「困难认定审核」）。
2. **待办**：当前轮到你这一级、且在你数据范围内的申请。
3. 点进详情：看家庭情况、特殊群体、附件、签字。附件在磁盘上，点下载即可，不要只看有没有记录。
4. **通过**：填写意见，班主任/系里通常要选定或确认困难等级。
5. **退回**：必须写意见；可退回学生重填，或退回比你低的一级重审。
6. 自己这一级刚审过、下一级还没动时，可以**撤回**自己的通过/退回，申请回到你的待办。

批量通过/退回只处理待办里勾选的记录，仍然受数据范围限制。

「审核记录」里能看到已经经过你这级、或你这级职责范围内的历史件，便于抽查，不是第二份待办。

### 4.4 助学金（国家助学金）

- 必须已有**同一学年、已通过**的困难认定，否则无法创建。
- 创建时从认定表预填家庭等信息，学生核对后提交。
- 审核路径与认定相同：班主任 → 教学系 → 资助中心。
- 通过后可导出助学金申请表（Word）。认定通过后可导出认定申请表（PDF）以及认定结果汇总表（Excel，管理员/有权限角色按页面提供的入口操作）。

助学金通过不等于认定等级变化。要改困难等级，只能走认定的退回或按学校规定在认定环节调整。

### 4.5 学年与重复申请

认定、助学金都带学年。同一学生同一学年同一类型不要重复交；系统会拦截。换学年视为新一轮，基础数据（班级、班主任）要先按新年级调整好，再让学生填报。

### 4.6 审核过程中不要做的事

- 不要在审核中途删除学生、改学号、把学生调到未挂班主任的班级：待办会「消失」或落到别人身上。
- 不要用管理员账号代替班主任批量通过后，又把班主任名册改掉，造成责任对不上。
- 不要只导库不恢复附件：审核详情里的证明材料会全部失效。

---

## 5. 班主任使用说明

你的角色是 `classadvisor`。登录后主要用：**工作台、学生信息、困难认定审核、助学金审核**。

### 5.1 你能做什么

- 查看所管班级的学生名册（只读，不能导入、不能改档案）。
- 审核本班已提交的认定、助学金：通过或退回。
- 在通过认定时提出或确认困难等级。
- 查看本班学生的申请详情和附件。

你不能：改院系/班级结构、改学生身份证和学号、管外班的申请、开用户账号。这些找系统管理员。

### 5.2 开学后先核对范围

1. 打开「学生信息」，人数、名单是否与纸质班册一致。
2. 若名单为空或少人：多半是管理员还没把班级挂到你的教工号，或学生导进了另一个班名。不要自己「再导一遍学生」，把问题反馈给管理员。
3. 工作台待办为 0 不一定是没人申请，也可能学生还在草稿、或挂错班。

### 5.3 认定审核建议顺序

1. 待办列表按班、按学号看一遍。
2. 打开详情：家庭成员人数是否对、特殊群体是否有证明、有没有签字。
3. 材料不足：退回学生并写清缺什么（例如「请补低保证明」），不要口头说了却不退回，否则申请一直占着待办。
4. 材料齐全：通过并选择困难等级。通过后进入教学系待办，你这边待办消失。
5. 若点错通过且系里还没审，用「撤回」拿回来。

助学金审核看的是认定是否已通过、申请学年是否一致、信息是否被学生改乱。认定没过的学生进不了助学金流程。

### 5.4 导出

学生信息页可以导出本范围内的名册（全部 / 筛选 / 勾选）。导出文件给教务或资助中心用可以，**不要把导出文件再导入**——班主任没有导入权限；交给管理员时说明「仅供核对，不要当全量覆盖」。

---

## 6. 系管理员使用说明

你的角色是 `department`（教学系经办人）。数据范围是**账号上所属院系**下的全部班级，不是某一个班。

### 6.1 你能做什么

- 只读查看本系学生。
- 审核班主任已通过、进入「待教学系评审」的认定和助学金。
- 退回学生或退回班级重审。
- 调整或确认困难等级后送资助中心。

你不能改基础数据、不能给学生开户。本系班主任挂错班、学生导错系，一律提给系统管理员。

### 6.2 和班主任的分工

| 环节 | 班主任 | 系管理员 |
| --- | --- | --- |
| 材料是否齐全、是否本班学生 | 第一责任 | 抽查 |
| 困难等级是否符合本系尺度 | 初拟 | 复核、可改 |
| 退回补材料 | 可退学生 | 可退学生或退回班级 |
| 外系学生 | 不可见 | 不可见 |

系里不要审「待班级评审」的件——那不在你的待办里。若班主任长期不审，协调对方登录，而不是用管理员绕过（除非学校明确授权系统管理员代审）。

### 6.3 本系数据范围怎么来的

用户管理里该账号的「所属院系」必须指向正确院系。院系填错会出现：待办全空，或看到的是另一个系。改院系属于系统管理员操作。

班级改名、学生转系后，历史申请仍挂在原学生记录上。转系应先办完当轮审核，再改学生院系/班级，避免待办跳到新班主任而意见还是旧班的。

### 6.4 导出与核对

可按院系筛选学生后「导出筛选」，用于系内核对认定进度。不要把导出表当作可导入的权威源改完再扔给管理员全量导入，除非已经核对学号、班级名称与系统完全一致。

---

## 7. 系统管理员使用说明

你的角色是 `admin`。除基础数据（第 3 章）和审核（第 4 章，全校范围）外，还负责账号、备份和年度收尾。

### 7.1 用户管理

- 为教学系、资助中心、管理员开账号；班主任账号与「班主任信息」教工号对应。
- 学生账号由学生信息自动创建，不要在这里批量开 `student`，以免和学号抢用户名。
- 导入用户：已存在用户名**跳过且不覆盖**。班主任 / 系管理员 / 学院管理员必须带手机号，初始密码分别为 `Adv` / `Dept` / `Aid` + 手机号后 6 位。改密码用「重置密码」（可按同一规则重置）。
- 禁用账号可阻止登录，不会删除历史审核记录。
- 教学系账号必须带所属院系，否则本系待办为空。

### 7.2 数据备份与恢复

菜单：**系统管理 → 数据备份**。

- 「立即备份」默认打包**全部业务表 + 上传附件**，生成 zip 存在服务器备份目录。
- 务必再点「下载」，把 zip 拷到本机或学校规定的网盘。只留在服务器上挡不住磁盘损坏。
- 「恢复」会清空当前库再写入备份内容，备份之后的新申请、新学生会消失。界面要求输入「恢复」二字。恢复前系统会自动再做一份 `prerestore-*.zip`，恢复错了可以对它再恢复。
- 「上传备份并恢复」用于新服务器重建。新环境拉起服务后**不必先 seed**，直接用这份 zip 恢复即可。
- 取消「同时备份附件」会显著变小，但恢复后证明材料和签字图会丢。日常全量备份不要取消。

Docker 部署时数据在三个卷里：`pgdata`（库）、`uploads`（附件）、`backups`（备份 zip）。更新应用用 `docker compose up -d --build` 即可。**不要**对生产执行 `docker compose down -v`，那会删掉这三个卷。

### 7.3 年度工作建议节奏

1. 备份当前库并下载 zip。
2. 按第 2.2 节顺序更新院系/专业/年级/班主任/班级。
3. 导入或更新新生，抽查登录。
4. 导入本年度重点人群。
5. 通知班主任核对班级名单，再开放学生填报。
6. 认定结束后导出认定汇总，再开放助学金。
7. 全程不要在审核高峰改班级名称和学号。

### 7.4 代审与权限边界

管理员可以审全校待办，用于应急。日常仍应由班主任、系管理员、资助中心按级审，便于意见和责任对应到人。公示、名额预算、系统参数等菜单若显示为灰色，表示本期尚未开放，不要用改库的方式绕过。

### 7.5 出问题先对哪

| 现象 | 先查 |
| --- | --- |
| 班主任待办为空但学生说已提交 | 申请状态是否待班级评审；学生班级是否挂在该班主任名册 |
| 系管理员看不到本系 | 用户所属院系是否正确 |
| 学生导入大量失败 | 院系/专业/班级名称是否与系统一致；民族是否为字典名称；表头是否被改 |
| 班级导入失败 | 院系编码、专业编码、入学年份、教工号是否已存在 |
| 学生无法登录 | 是否已导入学生；用户名是否为学号；密码是否为 `Stu`+身份证后 6 位；账号是否被禁用 |
| 班主任无法登录 | 用户名是否为教工号；密码是否为 `Adv`+手机号后 6 位；手机号是否填对 |
| 系管理员 / 学院管理员无法登录 | 角色是否分别为 `department` / `aidcenter`；密码是否为 `Dept` / `Aid` + 手机号后 6 位 |
| 认定通过了却不能申请助学金 | 是否同一学年；认定状态是否为通过 |
| 审核能看记录但附件打不开 | 备份/迁移时是否还原了 `uploads`；是否用了「不含附件」的备份 |
| 导入提示用户名已存在 | 学号与某个职工账号冲突，或重复导入用户（用户导入不覆盖） |

---

## 8. 资助中心（补充）

角色 `aidcenter` 的菜单与班主任、系管理员相同，但数据范围是全校，负责认定和助学金的**终审**。操作同第 4 章。终审通过后认定等级生效，学生才能走助学金；终审退回可退到系、班或学生。汇总表、正式 PDF/Word 导出以页面提供的按钮为准。

---

## 9. 相关文档

| 文档 | 内容 |
| --- | --- |
| [README.md](./README.md) | 仓库说明、本地启动、演示账号 |
| [deploy/README.md](./deploy/README.md) | Docker 部署、卷与备份运维 |
| [backend/docs/API.md](./backend/docs/API.md) | 接口字段（给开发 / 对接） |
